module.exports = {
  rules: {
    // always use .jsx or .tsx in react components
    "react/jsx-filename-extension": [1, { extensions: [".tsx", ".jsx"] }],

    // allow use before define
    "no-use-before-define": "off",
  },
};
